# XcodeGen project spec — run `xcodegen generate` to produce GuitarixMac.xcodeproj
# https://github.com/yonaskolb/XcodeGen

name: GuitarixMac
options:
  bundleIdPrefix: com.guitarix
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"
  createIntermediateGroups: true

settings:
  SWIFT_VERSION: "5.9"
  MACOSX_DEPLOYMENT_TARGET: "13.0"
  ENABLE_HARDENED_RUNTIME: YES
  ENABLE_USER_SELECTED_FILES: readOnly     # for IR file picker
  LIBRARY_SEARCH_PATHS: "$(inherited) $(PROJECT_DIR)/build/lib"
  HEADER_SEARCH_PATHS: >
    $(inherited)
    $(PROJECT_DIR)/build/include/guitarix
    $(PROJECT_DIR)/Sources/Bridge
    /opt/homebrew/include
    /usr/local/include

targets:

  # ── Main macOS application ─────────────────────────────────────────────────
  GuitarixMac:
    type: application
    platform: macOS
    sources:
      - Sources/App
      - path: Sources/Bridge
        compilerFlags: "-x objective-c++"
    dependencies:
      - sdk: CoreAudio.framework
      - sdk: AudioToolbox.framework
      - sdk: AudioUnit.framework
      - sdk: Foundation.framework
      - sdk: AppKit.framework
      - library: build/lib/libguitarix_engine.a
        embed: false
      - library: build/lib/libguitarix_coreaudio.a
        embed: false
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.guitarix.GuitarixMac
      INFOPLIST_FILE: Resources/Info.plist
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      # Obj-C++ for the bridge files
      GCC_INPUT_FILETYPE: automatic
      CLANG_CXX_LANGUAGE_STANDARD: "c++17"
      CLANG_CXX_LIBRARY: "libc++"
      # Link Homebrew dynamic libs
      OTHER_LDFLAGS: >
        -L/opt/homebrew/lib
        -L/usr/local/lib
        -lglibmm-2.4
        -lgobject-2.0
        -lglib-2.0
        -lsigc-2.0
        -lsndfile
        -lfftw3f
        -lboost_thread-mt
        -lboost_iostreams
        -ldl
      SWIFT_OBJC_BRIDGING_HEADER: Sources/Bridge/GuitarixMac-Bridging-Header.h
